{
  "name": "nglonat-scripts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "migrate": "psql $DATABASE_URL -f db/rebuild_tables.sql",
    "seed": "psql $DATABASE_URL -f db/seed.sql"
  }
}
