{
  "name": "nglonat-scripts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "migrate": "psql postgresql://ama63:كلمة_المرور@localhost:5432/postgres -f db/rebuild_tables.sql",
    "seed": "psql -f db/seed.sql"
  }
}
